import { COLOR } from '@theme/colors';
import { FONTS } from '@theme/fonts';

export const REACT_NATIVE_PAPER_INPUT_THEME = {
  fonts: { regular: { fontFamily: FONTS.family.lightBoreal } },
  colors: { text: COLOR.font.lightGrey },
};
