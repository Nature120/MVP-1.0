import styled from 'styled-components/native';

import { COLOR } from '@theme/colors';

export const MediaPlayerSheetStyled = {
  Wrapper: styled.View`
    flex: 1;
    background-color: ${COLOR.background.darkWhite};
  `,
};
