import { ViewStyle } from 'react-native';

export const thumbStyles: ViewStyle = {
  backgroundColor: 'white',
  justifyContent: 'center',
  alignItems: 'center',
  width: 30,
  height: 30,
  borderRadius: 50,
};

export const thumbTouchSize = { width: 30, height: 30 };
