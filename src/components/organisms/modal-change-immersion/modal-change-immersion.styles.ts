import { Title } from 'react-native-paper';
import styled from 'styled-components';

import { FONTS } from '@theme/fonts';

export const StyledModalChangeImmersion = {
  ModalText: styled(Title)`
    font-size: ${FONTS.size.xLmedium}px;
    text-align: center;
  `,
};
